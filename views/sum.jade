extends layout

block content
    h1= title
    form(action="/api/sum", method="post")
        input(type="text", name="a", placeholder="Enter number1", value="#{a || ''}")
        input(type="text", name="b", placeholder="Enter number2", value="#{b || ''}")
        button(type="submit") Calculate
    p Result: #{result}